name: Build, Test and Deploy Lambda

permissions:
  id-token: write
  contents: read

on:
  workflow_dispatch:
    inputs:
      deploy_only:
        description: 'Pular testes e executar apenas o deploy (true/false)'
        required: false
        default: false
        type: boolean

  push:
    branches:
      - develop
    paths:
      - 'src/**'
      - 'tests/**'
  pull_request:
    branches:
      - master
    paths:
      - 'src/**'
      - 'tests/**'

env:
  AWS_REGION: ${{ vars.AWS_REGION }}
  AWS_ACCOUNT_ID: ${{ secrets.AWS_ACCOUNT_ID }}
  PYTHON_VERSION: '3.12'
  S3_BUCKET_NAME: ${{ vars.S3_BUCKET_NAME }}
  S3_BUCKET_DIR_UPLOADS: ${{ vars.S3_BUCKET_DIR_UPLOADS }}
  S3_BUCKET_DIR_FINISHED: ${{ vars.S3_BUCKET_DIR_FINISHED }}
  S3_BUCKET_DIR_PROCESSING: ${{ vars.S3_BUCKET_DIR_PROCESSING }}
  EVENT_BUS_NAME: ${{ vars.EVENT_BUS_NAME }}
  DYNAMODB_TABLE_NAME: ${{ vars.DYNAMODB_TABLE_NAME }}
  DYNAMODB_STREAM_ARN: ${{ vars.DYNAMODB_STREAM_ARN }}
  VDSC_PNG_COMPRESSION_LEVEL: ${{ vars.VDSC_PNG_COMPRESSION_LEVEL }}
  VDSC_ZIP_COMPRESSION_LEVEL: ${{ vars.VDSC_ZIP_COMPRESSION_LEVEL }}
  VDSC_QUALITY: ${{ vars.VDSC_QUALITY }}
  VDSC_MAX_WORKERS: ${{ vars.VDSC_MAX_WORKERS }}
  VDSC_MAX_TIMEOUT: ${{ vars.VDSC_MAX_TIMEOUT }}
  AWS_LAMBDA_LOG_LEVEL: ${{ vars.AWS_LAMBDA_LOG_LEVEL }}

jobs:
  tests-and-coverage:
    name: Testes e Quality Gate
    runs-on: ubuntu-latest
    if: (github.event.inputs.deploy_only == 'false' &&  github.event_name == 'workflow_dispatch') || (github.event_name == 'push' || github.event_name == 'pull_request')
    steps:
      - name: Checkout do código
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configurar SSH para autenticação com repositórios privados
        run: |
          echo "🔐 Configurando SSH para repositórios privados..."
          
          # Criar diretório .ssh se não existir
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          
          # Adicionar GitHub ao known_hosts para evitar prompts interativos
          ssh-keyscan -t rsa github.com >> ~/.ssh/known_hosts 2>/dev/null || true
          
          # Configurar git para usar SSH
          git config --global url."git@github.com:".insteadOf "https://github.com/"
          
          echo "✅ Configuração SSH concluída"

      - name: Determinar branch do video-slice-core
        id: determine-branch
        run: |
          # Determinar a branch atual
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            BRANCH="${{ github.head_ref }}"
            echo "📌 Branch detectada (pull_request): ${BRANCH}"
          else
            BRANCH="${{ github.ref_name }}"
            echo "📌 Branch detectada (push/workflow_dispatch): ${BRANCH}"
          fi
          
          # Mapear branch para video-slice-core
          # Se for develop, usar develop; se for master, usar main; caso contrário, usar a mesma branch
          case "${BRANCH}" in
            develop)
              CORE_BRANCH="develop"
              echo "✅ Usando video-slice-core@develop"
              ;;
            master)
              CORE_BRANCH="main"
              echo "✅ Usando video-slice-core@main"
              ;;
            *)
              CORE_BRANCH="${BRANCH}"
              echo "✅ Usando video-slice-core@${BRANCH}"
              ;;
          esac
          
          echo "CORE_BRANCH=${CORE_BRANCH}" >> $GITHUB_OUTPUT

      - name: Configurar Python ${{ vars.PYTHON_VERSION }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'

      - name: Cache de dependências Python
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Atualizar requirements.txt com branch correto
        run: |
          CORE_BRANCH="${{ steps.determine-branch.outputs.CORE_BRANCH }}"
          echo "🔄 Atualizando requirements.txt para usar video-slice-core@${CORE_BRANCH}"
          
          # Criar backup do arquivo original
          cp src/requirements.txt src/requirements.txt.backup
          
          # Substituir a linha de vdsc-core no requirements.txt
          sed -i "s|vdsc-core @ git+ssh://git@github.com/11soat-hackton-videoslice/video-slice-core.git@.*|vdsc-core @ git+ssh://git@github.com/11soat-hackton-videoslice/video-slice-core.git@${CORE_BRANCH}|g" src/requirements.txt
          
          echo "📋 Conteúdo atualizado de requirements.txt:"
          cat src/requirements.txt
          
          # Validar que o arquivo foi atualizado corretamente
          echo ""
          echo "🔍 Validando requirements.txt..."
          if grep -q "vdsc-core @ git+ssh://git@github.com/11soat-hackton-videoslice/video-slice-core.git@${CORE_BRANCH}" src/requirements.txt; then
            echo "✅ Branch correta detectada no requirements.txt"
          else
            echo "❌ ERRO: Branch não foi atualizada corretamente em requirements.txt"
            echo "📋 Comparando original com atualizado:"
            echo "--- Original:"
            cat src/requirements.txt.backup
            echo "--- Atualizado:"
            cat src/requirements.txt
            exit 1
          fi
          
          # Validar linhas vazias e sintaxe
          if grep -q "^[[:space:]]*$" src/requirements.txt; then
            echo "⚠️ Aviso: Removendo linhas em branco extras..."
            sed -i '/^[[:space:]]*$/d' src/requirements.txt
          fi

      - name: Instalar dependências
        run: |
          echo "📦 Iniciando instalação de dependências..."
          python -m pip install --upgrade pip
          echo "✅ pip atualizado com sucesso"
          
          echo ""
          echo "📦 Instalando dependências de teste..."
          pip install pytest pytest-cov pytest-mock boto3 moto requests opencv-python-headless
          echo "✅ Dependências de teste instaladas"
          
          if [ -f src/requirements.txt ]; then
            echo ""
            echo "📦 Instalando dependências da aplicação (incluindo vdsc-core)..."
            pip install -r src/requirements.txt
            
            if [ $? -ne 0 ]; then
              echo "❌ ERRO: Falha ao instalar dependências de src/requirements.txt"
              echo "🔍 Tentando diagnóstico..."
              echo "Conteúdo de src/requirements.txt:"
              cat src/requirements.txt
              exit 1
            fi
            echo "✅ Dependências da aplicação instaladas com sucesso"
          else
            echo "❌ ERRO: Arquivo src/requirements.txt não encontrado"
            exit 1
          fi

      - name: Validar instalação do vdsc-core
        run: |
          echo "🔍 Validando instalação do vdsc-core..."
          python << 'EOF'
          try:
              import core
              import vdsc_core
              print("✅ vdsc-core importado com sucesso")
              print(f"   Localização: {core.__file__}")
              
              # Tentar obter versão se disponível
              if hasattr(core, '__version__'):
                  print(f"   Versão: {core.__version__}")
          except ImportError as e:
              print(f"❌ ERRO: Falha ao importar vdsc-core: {e}")
              exit(1)
          EOF
          
          if [ $? -ne 0 ]; then
            echo "❌ Validação do vdsc-core falhou"
            exit 1
          fi
          echo "✅ vdsc-core validado com sucesso"

      - name: Executar testes com cobertura
        run: |
          export PYTHONPATH=$PYTHONPATH:$(pwd)/src
          pytest tests/ --cov=src --cov-report=xml:coverage.xml --cov-report=html --junitxml=test-results.xml -v

      - name: Verificar e corrigir arquivo de cobertura
        run: |
          ls -la
          if [ -f coverage.xml ]; then
            echo "✅ coverage.xml encontrado"
            echo "📋 Conteúdo antes da correção:"
            head -30 coverage.xml
            
            echo ""
            echo "🔧 Corrigindo caminhos absolutos no coverage.xml..."
            # Remover completamente a seção <sources>...</sources>
            sed -i '/<sources>/,/<\/sources>/d' coverage.xml
            # Adicionar prefixo src/ aos filenames que não começam com src/
            sed -i 's|filename="|filename="src/|g' coverage.xml
            # Corrigir casos onde src/ foi duplicado
            sed -i 's|filename="src/src/|filename="src/|g' coverage.xml
            
            echo ""
            echo "📋 Conteúdo após a correção:"
            head -30 coverage.xml
          else
            echo "❌ coverage.xml NÃO encontrado"
            exit 1
          fi

      - name: Upload de relatório de cobertura
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: |
            coverage.xml
            htmlcov/
            test-results.xml

      - name: Análise do SonarCloud
        uses: SonarSource/sonarcloud-github-action@v3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

  approval:
    name: Aprovação Manual
    runs-on: ubuntu-latest
    needs: tests-and-coverage
    if: github.event_name == 'pull_request'
    environment:
      name: production-approval
    steps:
      - name: Manual Approval
        uses: trstringer/manual-approval@v1
        with:
          secret: ${{ secrets.GITHUB_TOKEN }}
          approvers: gustavo-log,titoparizotto
          minimum-approvals: 1

  build-and-deploy:
    name: Build e Deploy na AWS
    runs-on: ubuntu-latest
    needs: [tests-and-coverage, approval]
    if: |
      always() &&
        (needs.tests-and-coverage.result == 'success' || 
          (needs.tests-and-coverage.result == 'skipped' &&  github.event.inputs.deploy_only == 'true')
        )&&
      (
        ((github.event_name == 'push' || github.event_name == 'workflow_dispatch') && github.ref == 'refs/heads/develop') ||
        (github.event_name == 'pull_request' && needs.approval.result == 'success')
      ) &&
      (needs.approval.result == 'success' || needs.approval.result == 'skipped')
    # Se for push na develop: executa direto após tests-and-coverage (approval será skipped)
    # Se for pull_request para master: só executa após aprovação manual bem-sucedida

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Configurar SSH para autenticação com repositórios privados
        run: |
          echo "🔐 Configurando SSH para repositórios privados..."
          
          # Criar diretório .ssh se não existir
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          
          # Adicionar GitHub ao known_hosts para evitar prompts interativos
          ssh-keyscan -t rsa github.com >> ~/.ssh/known_hosts 2>/dev/null || true
          
          # Configurar git para usar SSH
          git config --global url."git@github.com:".insteadOf "https://github.com/"
          
          echo "✅ Configuração SSH concluída"

      - name: Determinar branch do video-slice-core
        id: determine-branch
        run: |
          # Determinar a branch atual
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            BRANCH="${{ github.head_ref }}"
            echo "📌 Branch detectada (pull_request): ${BRANCH}"
          else
            BRANCH="${{ github.ref_name }}"
            echo "📌 Branch detectada (push/workflow_dispatch): ${BRANCH}"
          fi
          
          # Mapear branch para video-slice-core
          # Se for develop, usar develop; se for master, usar main; caso contrário, usar a mesma branch
          case "${BRANCH}" in
            develop)
              CORE_BRANCH="develop"
              echo "✅ Usando video-slice-core@develop"
              ;;
            master)
              CORE_BRANCH="main"
              echo "✅ Usando video-slice-core@master"
              ;;
            *)
              CORE_BRANCH="${BRANCH}"
              echo "✅ Usando video-slice-core@${BRANCH}"
              ;;
          esac
          
          echo "CORE_BRANCH=${CORE_BRANCH}" >> $GITHUB_OUTPUT

      - name: Configurar Python ${{ vars.PYTHON_VERSION }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'

      - name: Atualizar requirements.txt com branch correto
        run: |
          CORE_BRANCH="${{ steps.determine-branch.outputs.CORE_BRANCH }}"
          echo "🔄 Atualizando requirements.txt para usar video-slice-core@${CORE_BRANCH}"
          
          # Criar backup do arquivo original
          cp src/requirements.txt src/requirements.txt.backup
          
          # Substituir a linha de vdsc-core no requirements.txt
          sed -i "s|vdsc-core @ git+ssh://git@github.com/11soat-hackton-videoslice/video-slice-core.git@.*|vdsc-core @ git+ssh://git@github.com/11soat-hackton-videoslice/video-slice-core.git@${CORE_BRANCH}|g" src/requirements.txt
          
          echo "📋 Conteúdo atualizado de requirements.txt:"
          cat src/requirements.txt
          
          # Validar que o arquivo foi atualizado corretamente
          echo ""
          echo "🔍 Validando requirements.txt..."
          if grep -q "vdsc-core @ git+ssh://git@github.com/11soat-hackton-videoslice/video-slice-core.git@${CORE_BRANCH}" src/requirements.txt; then
            echo "✅ Branch correta detectada no requirements.txt"
          else
            echo "❌ ERRO: Branch não foi atualizada corretamente em requirements.txt"
            echo "📋 Comparando original com atualizado:"
            echo "--- Original:"
            cat src/requirements.txt.backup
            echo "--- Atualizado:"
            cat src/requirements.txt
            exit 1
          fi
          
          # Validar linhas vazias e sintaxe
          if grep -q "^[[:space:]]*$" src/requirements.txt; then
            echo "⚠️ Aviso: Removendo linhas em branco extras..."
            sed -i '/^[[:space:]]*$/d' src/requirements.txt
          fi

      - name: Configurar AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_TO_ASSUME }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Validar credenciais AWS
        run: |
          echo "🔍 Validando configuração AWS..."
          aws sts get-caller-identity
          echo "✅ Credenciais AWS configuradas corretamente"

      - name: Preparar pacote de build-and-deploy
        run: |
          echo "📦 Preparando pacote de build-and-deploy..."
          mkdir -p lambda-package
          
          if [ -f src/requirements.txt ]; then
            echo "📋 Instalando dependências de src/requirements.txt..."
            echo "🔍 Conteúdo do requirements.txt:"
            cat src/requirements.txt
            echo ""
            
            pip install -r src/requirements.txt -t lambda-package/ --quiet --no-cache-dir
            
            if [ $? -ne 0 ]; then
              echo "❌ ERRO: Falha ao instalar dependências para o pacote Lambda"
              exit 1
            fi
            
            echo "✅ Dependências instaladas no lambda-package"
            
            # Validar que vdsc-core foi instalado
            if [ -d "lambda-package/core" ]; then
              echo "✅ vdsc-core detectado no pacote Lambda"
              ls -la lambda-package/core/
            else
              echo "⚠️ Aviso: Diretório 'core' não encontrado no lambda-package (pode estar instalado de outra forma)"
            fi
          else
            echo "❌ ERRO: Arquivo requirements.txt não encontrado"
            exit 1
          fi
          
          echo ""
          echo "📋 Copiando código fonte..."
          cp -r src/* lambda-package/
          echo "✅ Código fonte copiado"
          
          echo ""
          echo "🧹 Removendo arquivos desnecessários para reduzir tamanho..."
          # Remover arquivos de cache e temporários
          find lambda-package -type d -name "__pycache__" -exec rm -rf {} + 2>/dev/null || true
          find lambda-package -type d -name "*.dist-info" -exec rm -rf {} + 2>/dev/null || true
          find lambda-package -type d -name "*.egg-info" -exec rm -rf {} + 2>/dev/null || true
          find lambda-package -type f -name "*.pyc" -delete 2>/dev/null || true
          find lambda-package -type f -name "*.pyo" -delete 2>/dev/null || true
          find lambda-package -type f -name "*.so" -exec strip {} \; 2>/dev/null || true
          
          # Remover testes e documentação de dependências
          find lambda-package -type d -name "tests" -exec rm -rf {} + 2>/dev/null || true
          find lambda-package -type d -name "test" -exec rm -rf {} + 2>/dev/null || true
          find lambda-package -type f -name "*.md" -delete 2>/dev/null || true
          find lambda-package -type f -name "*.txt" ! -name "requirements.txt" -delete 2>/dev/null || true
          
          # Remover dependências de teste se presentes
          rm -rf lambda-package/pytest* 2>/dev/null || true
          rm -rf lambda-package/moto* 2>/dev/null || true
          rm -rf lambda-package/_pytest* 2>/dev/null || true
          rm -rf lambda-package/coverage* 2>/dev/null || true
          
          echo "✅ Arquivos desnecessários removidos"
          echo ""
          echo "🗜️ Criando arquivo ZIP..."
          cd lambda-package
          zip -r ../lambda-build-and-deployment-package.zip . -q -9
          cd ..
          
          PACKAGE_SIZE=$(ls -lh lambda-build-and-deployment-package.zip | awk '{print $5}')
          echo "📊 Tamanho do pacote final: ${PACKAGE_SIZE}"
          echo "✅ Pacote preparado com sucesso"
        env:
          PYTHON_REQUIRIMENTS: ${{ github.workspace }}/src/requirements.txt || {{vars.PYTHON_REQUIRIMENTS}}

      - name: Deploy via AWS CLI
        run: |
          echo "🚀 Iniciando build-and-deploy da Lambda via AWS CLI..."
          TIMESTAMP=$(date +%Y-%m-%dT%H:%M:%SZ)
          FUNCTION_NAME="vdsc-prd-lmb-video-slice"
          
          # Verificar tamanho do pacote
          PACKAGE_SIZE_BYTES=$(stat -c%s lambda-build-and-deployment-package.zip)
          PACKAGE_SIZE_MB=$((PACKAGE_SIZE_BYTES / 1024 / 1024)) 
          echo "📊 Tamanho do pacote: ${PACKAGE_SIZE_MB}MB (${PACKAGE_SIZE_BYTES} bytes)"
          
          echo "⬆️ Fazendo upload do pacote para S3..."
          S3_KEY="deploy/lambda/vdsc-prd-lmb-video-slice-${TIMESTAMP}.zip"
            
          aws s3 cp lambda-build-and-deployment-package.zip \
            s3://${{ env.S3_BUCKET_NAME }}/${S3_KEY} \
            --region ${{ env.AWS_REGION }} \
            --no-progress
            
          echo "✅ Upload para S3 concluído: s3://${{ env.S3_BUCKET_NAME }}/${S3_KEY}"
          echo ""
          echo "📤 Atualizando código da função Lambda via S3..."
            
          aws lambda update-function-code \
            --function-name ${FUNCTION_NAME} \
            --s3-bucket ${{ env.S3_BUCKET_NAME }} \
            --s3-key ${S3_KEY} \
            --region ${{ env.AWS_REGION }} \
            --no-cli-pager
          
          echo "⏳ Aguardando conclusão da atualização do código..."
          aws lambda wait function-updated \
            --function-name ${FUNCTION_NAME} \
            --region ${{ env.AWS_REGION }}
          
          echo "✅ Código atualizado com sucesso às ${TIMESTAMP}"
          
          echo ""
          echo "⚙️ Atualizando configurações da Lambda..."
          
         #    # Criar arquivo JSON com as variáveis de ambiente usando jq
         #    jq -n \
         #      --arg s3_bucket "${{ env.S3_BUCKET_NAME }}" \
         #      --arg s3_uploads "${{ env.S3_BUCKET_DIR_UPLOADS }}" \
         #      --arg s3_finished "${{ env.S3_BUCKET_DIR_FINISHED }}" \
         #      --arg s3_processing "${{ env.S3_BUCKET_DIR_PROCESSING }}" \
         #      --arg event_bus "${{ env.EVENT_BUS_NAME }}" \
         #      --arg dynamodb_table "${{ env.DYNAMODB_TABLE_NAME }}" \
         #      --arg png_compression "${{ env.VDSC_PNG_COMPRESSION_LEVEL }}" \
         #      --arg zip_compression "${{ env.VDSC_ZIP_COMPRESSION_LEVEL }}" \
         #      --arg quality "${{ env.VDSC_QUALITY }}" \
         #      --arg mpl_config "/tmp/matplotlib" \
         #      --arg max_workers "${{ env.VDSC_MAX_WORKERS }}" \
         #      --arg max_timeout "${{ env.VDSC_MAX_TIMEOUT }}" \
         #      --arg log_level "${{ env.AWS_LAMBDA_LOG_LEVEL }}" \
         #      '{Variables: {S3_BUCKET_NAME: $s3_bucket, S3_BUCKET_DIR_UPLOADS: $s3_uploads, S3_BUCKET_DIR_FINISHED: $s3_finished, S3_BUCKET_DIR_PROCESSING: $s3_processing, EVENT_BUS_NAME: $event_bus, DYNAMODB_TABLE_NAME: $dynamodb_table, VDSC_PNG_COMPRESSION_LEVEL: $png_compression, VDSC_ZIP_COMPRESSION_LEVEL: $zip_compression, VDSC_QUALITY: $quality, MPLCONFIGDIR: $mpl_config, VDSC_MAX_WORKERS: $max_workers, VDSC_MAX_TIMEOUT: $max_timeout, AWS_LAMBDA_LOG_LEVEL: $log_level}}' \
         #      > env-vars.json
         #    
         #    echo "📋 Variáveis de ambiente a serem configuradas:"
         #    cat env-vars.json
         #    
         #    aws lambda update-function-configuration \
         #      --function-name ${FUNCTION_NAME} \
         #      --region ${{ env.AWS_REGION }} \
         #      --environment file://env-vars.json \
         #      --handler app.lambda_handler \
         #      --timeout 30 \
         #      --memory-size 1769 \
         #      --ephemeral-storage Size=512 \
         #      --no-cli-pager
         #    
         #    # Limpar arquivo temporário
         #    rm -f env-vars.json
         #    
         #    echo "⏳ Aguardando conclusão da atualização de configuração..."
         #    aws lambda wait function-updated \
         #      --function-name ${FUNCTION_NAME} \
         #      --region ${{ env.AWS_REGION }}
         #    
         #    echo "✅ Configurações atualizadas com sucesso"
         #    echo ""
         #    echo "🎉 Deploy concluído com sucesso às ${TIMESTAMP}"
         #
         #- name: Configurar Event Source Mapping do DynamoDB
         #  run: |
         #    echo "🔗 Configurando Event Source Mapping do DynamoDB..."
         #    FUNCTION_NAME="vdsc-prd-lmb-video-slice"
         #    
         #    if [ -z "${{ env.DYNAMODB_STREAM_ARN }}" ]; then
         #      echo "⚠️ DYNAMODB_STREAM_ARN não configurado. Pulando configuração."
         #      exit 0
         #    fi
         #    
         #    echo "🔍 Verificando Event Source Mapping existente..."
         #    MAPPING_UUID=$(aws lambda list-event-source-mappings \
         #      --function-name ${FUNCTION_NAME} \
         #      --region ${{ env.AWS_REGION }} \
         #      --query "EventSourceMappings[?EventSourceArn=='${{ env.DYNAMODB_STREAM_ARN }}'].UUID" \
         #      --output text \
         #      --no-cli-pager)
         #    
         #    if [ -n "$MAPPING_UUID" ] && [ "$MAPPING_UUID" != "None" ]; then
         #      echo "✅ Event Source Mapping encontrado: ${MAPPING_UUID}"
         #      echo "🔄 Atualizando configuração..."
         #      
         #      aws lambda update-event-source-mapping \
         #        --uuid ${MAPPING_UUID} \
         #        --region ${{ env.AWS_REGION }} \
         #        --batch-size 1 \
         #        --maximum-batching-window-in-seconds 0 \
         #        --enabled \
         #        --no-cli-pager
         #      
         #      echo "✅ Event Source Mapping atualizado"
         #    else
         #      echo "➕ Criando novo Event Source Mapping..."
         #      
         #      # Criar arquivo JSON com os filtros usando jq
         #      jq -n '{
         #        Filters: [
         #          {
         #            Pattern: "{\"eventName\":[\"INSERT\"],\"dynamodb\":{\"NewImage\":{\"status\":{\"S\":[\"UPLOADED\"]}}}}"
         #          },
         #          {
         #            Pattern: "{\"eventName\":[\"MODIFY\"],\"dynamodb\":{\"NewImage\":{\"status\":{\"S\":[\"RETRYING\"]}}}}"
         #          }
         #        ]
         #      }' > filter_criteria.json
         #      
         #      echo "📋 Filtros a serem aplicados:"
         #      cat filter_criteria.json
         #      
         #      # Usar $LATEST para manter a Lambda sempre na última versão
         #      aws lambda create-event-source-mapping \
         #        --function-name ${FUNCTION_NAME} \
         #        --event-source-arn ${{ env.DYNAMODB_STREAM_ARN }} \
         #        --starting-position LATEST \
         #        --batch-size 1 \
         #        --maximum-batching-window-in-seconds 0 \
         #        --parallelization-factor 1 \
         #        --enabled \
         #        --filter-criteria file://filter_criteria.json \
         #        --region ${{ env.AWS_REGION }} \
         #        --no-cli-pager
         #      
         #      # Limpar arquivo temporário
         #      rm -f filter_criteria.json
         #      
         #      echo "✅ Event Source Mapping criado com sucesso"
         #    fi
         #
         #- name: Configurar Alias PRD apontando para $LATEST
         #  run: |
         #    echo "🏷️ Configurando alias PRD para apontar para \$LATEST..."
         #    FUNCTION_NAME="vdsc-prd-lmb-video-slice"
         #    
         #    # Verificar se o alias PRD já existe
         #    echo "🔍 Verificando se alias PRD existe..."
         #    ALIAS_EXISTS=$(aws lambda get-alias \
         #      --function-name ${FUNCTION_NAME} \
         #      --name PRD \
         #      --region ${{ env.AWS_REGION }} \
         #      --query 'Name' \
         #      --output text 2>/dev/null || echo "NOT_FOUND")
         #    
         #    if [ "$ALIAS_EXISTS" = "PRD" ]; then
         #      echo "✅ Alias PRD encontrado, atualizando para apontar para \$LATEST..."
         #      
         #      aws lambda update-alias \
         #        --function-name ${FUNCTION_NAME} \
         #        --name PRD \
         #        --function-version \$LATEST \
         #        --region ${{ env.AWS_REGION }} \
         #        --no-cli-pager
         #      
         #      echo "✅ Alias PRD atualizado para \$LATEST"
         #    else
         #      echo "➕ Alias PRD não encontrado, criando novo alias apontando para \$LATEST..."
         #      
         #      aws lambda create-alias \
         #        --function-name ${FUNCTION_NAME} \
         #        --name PRD \
         #        --function-version \$LATEST \
         #        --description "Alias de produção apontando para a versão \$LATEST" \
         #        --region ${{ env.AWS_REGION }} \
         #        --no-cli-pager
         #      
         #      echo "✅ Alias PRD criado e apontando para \$LATEST"
         #    fi
         #    
         #    # Verificar configuração final do alias
         #    echo ""
         #    echo "📋 Configuração final do alias PRD:"
         #    aws lambda get-alias \
         #      --function-name ${FUNCTION_NAME} \
         #      --name PRD \
         #      --region ${{ env.AWS_REGION }} \
         #      --query '{Name:Name, FunctionVersion:FunctionVersion, Description:Description}' \
         #      --output table \
         #      --no-cli-pager
         #
         #- name: Verificar status da função Lambda
         #  run: |
         #    echo "📋 Informações da função Lambda após build-and-deploy:"
         #    FUNCTION_NAME="vdsc-prd-lmb-video-slice"
         #    
         #    aws lambda get-function \
         #      --function-name ${FUNCTION_NAME} \
         #      --region ${{ env.AWS_REGION }} \
         #      --query 'Configuration.[FunctionName,Runtime,LastModified,State,Version,CodeSize,MemorySize,Timeout]' \
         #      --output table \
         #      --no-cli-pager
         #    
         #    echo ""
         #    echo "🏷️ Alias PRD configurado e apontando para \$LATEST"
         #    echo "✅ Deploy finalizado com sucesso"
